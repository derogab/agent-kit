# exit

A Pi plugin that adds `/exit` as an alias for `/quit`, gracefully closing Pi.

## Install

### Pi

Clone this repository, then install the plugin:

```bash
git clone https://github.com/derogab/agent-kit.git
pi install ./agent-kit/plugins/exit
```
